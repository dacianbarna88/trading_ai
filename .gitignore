__pycache__/
*.pyc
*.pyo
*.DS_Store
venv/
.env
*.log
*.pid

# Archives / backups
archive/
backups/
backup_v41_pre_market/
restore_2026_06_22/

# Generated logs / runtime data
*.csv
*.txt
*.pdf
*.json
bot_status.txt
bot_pid.txt

# ...but not source-controlled manifests that happen to be .txt
!requirements.txt

# ...and not the committed ROI-queue bootstrap snapshot, which happens
# to be .json (2026-09-22) -- production's last-known-good recovery data
# for the gitignored runtime tae_roi_queue.json, read by
# tae_roi_queue_ssot.py and tae_roi_queue_ssot_test.py
!tae_roi_queue_bootstrap_snapshot.json

# Keep source code and markdown tracked manually

# Python cache
__pycache__/
*.pyc

# Generated/reproducible data — not source code
runtime_outputs/
research_outputs/
audit/
*.parquet
*.jsonl
*.lock
